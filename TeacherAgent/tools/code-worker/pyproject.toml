[project]
name = "code-worker"
version = "0.1.0"
description = "TeacherAgent safe Python code runner"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
