{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for TeacherAgent.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
